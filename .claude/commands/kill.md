# /kill — Emergency Stop

Immediately stop all active sub-agents and background work.

## Steps (execute in order, no confirmation needed)

1. List and kill all active sub-agents
2. Run: bash /home/elliotbot/clawd/scripts/kill_all.sh
3. Reply exactly: "All stopped. What's wrong?"
4. Do nothing else until Dave gives the next instruction

This overrides everything. No questions. No "are you sure". No finishing current tasks.
