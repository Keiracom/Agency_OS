# Agency OS — Compiled LLM Wiki
> Auto-generated by `scripts/compile_llm_wiki.py` · regenerated weekly · DO NOT EDIT
> Built: 2026-05-12T04:00:49Z · Token cap: 2000

## 1. Stack
Python (FastAPI) backend on Railway · Next.js frontend on Vercel · Supabase Postgres (project `jatzvazlbusedwsnqxzr`) · Prefect orchestration · Redis queue · Pydantic AI agent framework. Repo `/home/elliotbot/clawd/Agency_OS`. Env `/home/elliotbot/.config/agency-os/.env`.

## 2. Pipeline (Siege Waterfall, F v2.1)
Flow A (sync, <6min): Verify ICP → DataForSEO T0 multi-category discovery → bulk insert lead_pool → ABN match (local JOIN) → activate campaign → fire Flow B.
Flow B (async, <10min): parallel `asyncio.gather` of T1 (BU ABN JOIN, local), T1.25 (ABR trading name), T1.5 (Bright Data LinkedIn Co), T2 (BD GMB backfill), T3 (Leadmagic email + Hunter L2 fallback ≥70), T-DM0 (DFS ad spend) → Stage 2 (person discovery) → Stage 2.5 (social, gated propensity ≥70) + T5 (Leadmagic mobile, gated propensity ≥85) → ALS scoring → Outreach.
Source: `src/pipeline/{discovery,email_waterfall,mobile_waterfall}.py`.

## 3. Live Vendors
| Vendor | Purpose |
|---|---|
| DataForSEO | T0 multi-category discovery (domain_metrics_by_categories) + T-DM0 ad spend + DM signals |
| Bright Data | T2 GMB backfill (gd_m8ebnr0q2qlklc02fz), T1.5 LinkedIn Company, T-DM2 LinkedIn Profile, ge |
| ABR (data.gov.au) | ABN + trading name |
| Leadmagic | Email + mobile |
| Jina AI Reader | Web scrape (free) |
| Anthropic API | Claude Haiku |
| Salesforge | Email outreach |
| Unipile | LinkedIn outreach |
| ElevenAgents | Voice AI (Alex) |
| Telnyx | SMS outreach |

## 4. Dead References
Never call: Clay, Hunter.io, Kaspr, Proxycurl, Apollo, Apify, Webshare, SERP API, Direct mail, ZeroBounce, Lemlist, SmartLead.
Hunter is dead EXCEPT as Pipeline F v2.1 L2 email fallback (score ≥70). See ARCH §5 T3.

## 5. Outreach Channels
Email = Salesforge (`SALESFORGE_API_KEY`). LinkedIn = Unipile. Voice = ElevenAgents (Alex agent). SMS = Telnyx (on hold until launch). Mail = DEAD (removed permanently).

## 6. Memory Architecture
SSOT: `public.agent_memories` (per-callsign, embeddings via OpenAI `text-embedding-3-small`, `supersedes_id` graph, ~6.3k rows). `public.ceo_memory` (kv state). `public.governance_events` (audit trail). DEPRECATED: `elliot_internal.memories` (kept warm for legacy reads). Mem0 cloud: RETIRED (see `docs/audits/memory_audit_2026-05-12_atlas_section.md`).

## 7. Governance — 7 Rules (Ratified 2026-05-01)
1 VERIFY · 2 COORDINATE · 3 APPROVE · 4 ORCHESTRATE · 5 COMMUNICATE · 6 GOVERN · 7 BUSINESS
Full text: `docs/governance/CONSOLIDATED_RULES.md`. Shared laws: `~/.claude/CLAUDE.md §Shared Governance`. Key invariants: Step 0 RESTATE mandatory (LAW XV-D); dual-CTO concur for merge; Skills > MCP > exec hierarchy (LAW VI); $AUD only (LAW II).

## 8. Active Services (systemd --user)
- `agency-os-opa`
- `agency-os-phoenix-export`
- `agency-os-slack-central-listener`
- `aiden-relay-watcher`
- `aiden-slack-mirror`
- `atlas-inbox-watcher`
- `atlas-relay-watcher`
- `coo-slack-listener`
- `max-relay-watcher`
- `orion-inbox-watcher`
- `orion-relay-watcher`

## 9. CEO State (live)
- Last directive: DASHBOARD-AUDIT-2026-04-28
- Roadmap key: `ceo:roadmap_20_capabilities_phases_2026-05-11` (read live for current phase)
- Max op state key: `ceo:max_operational_state` (read live)
Read fresh — never quote stale values; check `ceo:session_end_*` for last activity.

## 10. Callsign Hierarchy
`Dave → Claude (CEO) → Elliot (COO) → {Aiden, Max} (CTOs) → {Orion, Atlas} (engineers)`. Clones: ATLAS = Elliot's, ORION = Aiden's. Worktrees: `/home/elliotbot/clawd/Agency_OS{,-aiden,-atlas,-orion}`. Work flows DOWN; escalation flows UP. No cross-dispatch between clones.

## 11. Currency & Geography
**$AUD only.** Conversion: 1 USD = 1.55 AUD (LAW II). Target market: Australian SMBs (GMB-discovered). Pre-revenue (zero paying clients) — reject all social-proof claims unless Dave confirms (LAW pre_revenue).
