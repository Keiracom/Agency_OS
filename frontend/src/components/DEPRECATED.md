# ⚠️ DEPRECATED LOCATION

**This directory (`/src/components/`) is deprecated.**

All new components go in `/components/` (project root).

## Migration Status

- Sprint 1+ work: `/components/` only
- Existing code here: Will be migrated or removed in future sprints
- Do NOT add new files here

## Canonical Locations

```
/components/
├── layout/        ← Global layout (Sidebar, Header, AppShell)
├── onboarding/    ← Onboarding flow
├── dashboard/     ← Dashboard-specific
├── shared/        ← Cross-cutting utilities
└── ui/            ← shadcn/ui primitives
```

---
*Deprecated: 2026-02-10 (Sprint 1)*
