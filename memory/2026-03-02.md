# 2026-03-02 Session Log

## Directive #153 — ALS Score Migration (COMPLETED)

**PR #138 merged** at 11:37 UTC. Commit: `af8a03c`

### Phases Completed:
- **Phase 5**: Documentation — replaced 108 `als_score` references with `propensity_score` in docs/
- **Phase 6**: Migration 080 applied via Supabase MCP
  - Added `reachability_score` and `propensity_score` columns to `lead_pool` and `leads`
  - Backfilled from existing `als_score`
  - Created indexes
  - Updated SQLAlchemy models to use real column names (removed `name="als_score"` overrides)

### Test Count Analysis:
- **Main branch baseline**: 125 failed, 48 errors
- **PR #138 final**: 104 failed, 22 errors
- **Net improvement**: -21 failures, -26 errors
- The "41 failures" from prior session was incorrect (partial test run due to misconfigured pytest testpaths)

### Database State (Supabase prod):
- Project ID: `jatzvazlbusedwsnqxzr`
- Columns now exist: `als_score`, `reachability_score`, `propensity_score`
- Migration 078 was never applied (legacy column dance skipped)
- Migration 080 is the clean dual-score implementation

### Key Decisions:
- Kept `als_score` DB column for now (54 SQL references still use it)
- Python code uses `propensity_score` attribute mapped to `propensity_score` column
- `als_tier` → `propensity_tier` (still uses `als_tier` DB column via `name=` mapping)

### Next Steps:
- Dedicated directive for remaining 104 test failures (CEO stated: "Zero failures before launch")
- Eventually drop `als_score` column once all SQL refs migrated
