# Memory — 2026-03-16

## Session summary

### PRs merged today
- **PR #189** (Directive #198) — GMB capture overhaul: 14 fields, `open_website` fix, ABN BU match, T2.5 GMB Reviews wired. Merged 02:52Z
- **PR #190** (Directive #199) — Fix enrichment validation: `company_level=True` param, `"company"` key in `_enrich_tier1`, ALS wired in `_update_lead_from_enrichment`. Merged 04:50Z
- **PR #191** (Directive #200) — GMB passthrough enrichment: allow enrichment when `sources_used=0` but company data present. Merged 05:43Z
- **PR #192** (Directive #201) — **OPEN/PENDING MERGE** — Fix `enriched_at` datetime timezone: `TIMESTAMP WITHOUT TIME ZONE` column rejects timezone-aware datetime; fix is `.replace(tzinfo=None)`

### Root cause hunt: why 0 enriched after 20 runs
The enrichment pipeline had three sequential bugs fixed across PRs #190–#192:
1. `_validate_enrichment` gated on all 4 person fields — company-only GMB leads always failed (PR #190)
2. `sources_used > 0` gate discarded GMB data when T1 ABN returned 0 matches (PR #191)
3. `enriched_at` = `TIMESTAMP WITHOUT TIME ZONE` but code passed `datetime.now(UTC)` (timezone-aware) → asyncpg threw `can't subtract offset-naive and offset-aware datetimes` silently on every write (PR #192)

### E2E run history
- **v18** (03:01Z Mar 16): 0 enriched — root cause: bugs #1+#2+#3
- **v19** (04:50Z Mar 16): 0 enriched — PR #190 deployed, bugs #2+#3 still present
- **v20** (05:43Z Mar 16): 0 enriched — PRs #190+#191 deployed, bug #3 still present
- **v21**: PENDING — awaits PR #192 merge

### Verified locally
- `_enrich_single` on Melbourne Social Co: `enriched_at=2026-03-16 06:30:04` ✅, `propensity_score=13` ✅
- 765 passed, 4 skipped, 0 failed

### Remaining known issues (non-blocking)
- T1.5 LinkedIn Company fails: `'dict' object has no attribute 'rstrip'` — T1 ABN still fires (sources_used=2), SIZE_GATE HELD but enrichment writes OK
- audit_logs table missing columns `lead_company` and `domain` — non-blocking warnings
- `als_score` stays 0; only `propensity_score` is written by `_update_lead_from_enrichment`

## Next steps
1. Dave merge PR #192
2. Fire v21 E2E — first run with actual enriched_at + propensity_score distribution
3. Three-store saves for Directives #199, #200, #201 (deferred)
4. Fix T1.5 LinkedIn `'dict' object has no attribute 'rstrip'` bug (separate directive)
5. Fix audit_logs missing columns (migration needed)
