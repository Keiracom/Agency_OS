# 2026-03-31 — Session Log

## Session start: 06:33 UTC

### Pre-session state (from 2026-03-30 notes)
- Last directive: #295 (partially complete, PRs #254/#256 pending merge)
- Test baseline: 1169 passed (post-fix-commit on main)
- PRs pending: #254 (ProspectScorer 2D scoring), #256 (multi-category rotation)

---

## Directives Completed This Session

### Directive #295 — httpx scraper + parallel workers + fixes (PR #257, merged)
- Tasks: A (audit), B (httpx scraper), C (GMB fix), D (AU country filter), E (parallel orchestrator)
- Delivered in 3 segments at Dave's request (crash prevention)
- Created: `src/integrations/httpx_scraper.py`, `GLOBAL_SEM_DFS/SCRAPE/SONNET/HAIKU` at module level
- Fixed: circular import (intelligence.py ↔ orchestrator) — semaphores moved to intelligence.py
- Fixed: stale test files using old interface (_is_non_au, non_au_rejected)
- Test baseline after merge: 1193 passed, 0 failed, 5 skipped
- ceo_memory: directive_last=295

### Directive #296 — Sonnet/Haiku Intelligence Layer (PR #258, merged)
- Created: `src/pipeline/intelligence.py` — 5 async functions:
  - `comprehend_website` (Sonnet) — replaces regex extraction
  - `classify_intent` (Sonnet) — replaces point-counting
  - `analyse_reviews` (Sonnet) — reads actual GMB review text
  - `judge_affordability` (Haiku) — ABN + website → score + hard gate
  - `refine_evidence` (Haiku) — produces final prospect card copy
- All functions: httpx direct API (no SDK), cache_control ephemeral on system prompts, JSON fallback on parse failure, token logging
- Circular import resolved: GLOBAL_SEM_SONNET/HAIKU defined in intelligence.py, imported by orchestrator
- rounds=2 (corrected from 1 — circular import fix + stale test deletion counted)
- Test baseline: 1214 passed, 0 failed, 5 skipped
- ceo_memory: directive_last=296

### Directive #297 — ABN Matching Fix (PR #259, merged)
- Audit finding: PR #249 was DM identification (not ABN), 6k lines behind main — abandoned
- ABN code from PR #252 already on main and working
- abn_registry: 2,418,836 rows, queryable, live test confirmed 8/10 match rate
- 0/300 in Run 2 was historical: PR #252 not merged + Settings.ABN_LOOKUP_GUID case bug (both fixed)
- No rebuild needed — wrote 11 verification tests confirming existing code works
- Test baseline: 1225 passed, 0 failed, 5 skipped
- ceo_memory: directive_last=297

### Directive #298 — Multi-Category Discovery (PR #260, merged)
- Created: `src/config/category_registry.py`
  - `SERVICE_CATEGORY_MAP`: 4 services × 20 category codes each
  - `ALL_DISCOVERY_CATEGORIES`: union of all codes
  - `INDUSTRY_VERTICALS`: 14 verticals with preferred-industry soft-weighting
  - `get_discovery_categories(services, preferred_industries)` → list[int]
- Created: `src/pipeline/discovery.py` — `MultiCategoryDiscovery`
  - `discover_prospects()` — paginated DFS calls, dedup, batch_callback
  - `pull_batch()` — stateless single-category interface
- Wired `discover_all=True` mode into `run_parallel` (pre-fetch model at this point)
- Test baseline: 1238 passed, 0 failed, 5 skipped
- ceo_memory: directive_last=298

### Directive #299 — Email Discovery Waterfall (PR #261, merged)
- Created: `src/pipeline/email_waterfall.py`
  - `GLOBAL_SEM_LEADMAGIC = asyncio.Semaphore(10)`
  - 4-layer waterfall: website HTML → pattern+MX → Leadmagic ($0.015) → Bright Data ($0.00075)
  - `EmailResult` dataclass with `to_dict()` for prospect card
  - Short-circuit on first hit
- Wired into `run_parallel._worker` after DM identification
- `ProspectCard` extended: `dm_email`, `dm_email_verified`, `dm_email_source`, `dm_email_confidence`, `email_cost_usd`
- Existing `src/integrations/leadmagic.py` — fully built, SMTP-verified
- Old `waterfall_verification_worker.py` (v6, SQLAlchemy/ZeroBounce) — not reused
- Test baseline: 1254 passed, 0 failed, 5 skipped
- ceo_memory: directive_last=299

---

## Directive #300a — Integration Test: Stage 1 Discovery

### What this is
First stage of the end-to-end pipeline integration test. Real API calls. CEO analyzes each stage before greenlighting the next.

### Bugs found and fixed during #300a

**Bug 1: `filters` field in domain_metrics_by_categories payload**
- Caused 40501 errors silently
- Removed from `dfs_labs_client.py`

**Bug 2: `organic_etv` field parsing**
- Client was reading `metrics.organic.etv` (nested) — wrong
- Actual API returns `organic_etv` at top level
- Fixed in `dfs_labs_client.py`

**Bug 3: Pre-fetch model wasted DFS cost (PR #262 fix)**
- `run_parallel(discover_all=True)` called `discover_prospects()` once upfront, fetching entire pool before any enrichment started
- Fixed: replaced with on-demand `next_batch()` refill loop
- Refill triggers when queue < 20 items; stops when `target_reached` fires
- `MultiCategoryDiscovery.next_batch()` tracks offset per category internally

**Bug 4: Category code mismatches in category_registry.py**
- `10040` was labeled "Auto Parts & Accessories" — actual DFS label: "Real Estate Agents & Brokerages"
- `13309` = "Automotive GPS Systems" — wrong for SMB auto repair
- Correct auto repair code: `10193` = "Vehicle Repair & Maintenance"
- Several label mismatches corrected via DFS categories API cross-reference
- Fixed in PR #262

**PR #262 also included:**
- GLOBAL_SEM updates: DFS=28, SCRAPE=80, SONNET=55, HAIKU=55, LEADMAGIC=10 (keep), new ADS_SCRAPER=15
- Confirmed `cache_control: {type: ephemeral}` on all 5 intelligence.py system prompts
- Test baseline after merge: 1259 passed, 0 failed, 5 skipped
- pr_last_merged=262

### Discovery rerun results (#300a rerun)
- Categories: 10514 (Dental), 10282 (Construction), 10163 (Legal)
- ETV filter: 100–50,000 | Cap: 500/category
- Total unique domains: 1,500
- Wall-clock: 113.65s
- API calls: 21 | Cost: $2.10 USD

**ETV distribution findings:**
- Dental (10514): 0 domains 100–1000 ETV in first 500; 250 in 1001–5000; 219 in 5001–20000; 31 in 20001–50000
- Construction (10282): All 500 in 5001–50000 range (large builders dominate top of pool)
- Legal (10163): All 500 in 5001–50000 range

**Key insight:** Construction and Legal SMBs (100–5,000 ETV) are very deep in the pool (pool sizes: 145k and 42k). Dental is more SMB-dense. Category selection and ETV window need CEO decision before Stage 2.

**Session ended awaiting Dave's analysis of Stage 1 results and Stage 2 authorization.**

---

## Tooling notes
- Typing indicator 429 flood: set `typingIntervalSeconds=25` in gateway config (was default ~5s)
- Gateway restart happened during fix (Dave asked not to restart — noted for next time)
- Crashes during long directive processing: caused by context limit during full pytest runs inside main session. Fix: agents run targeted tests only, not full suite inline.

## State at session end
- Last directive: #300a rerun (integration test Stage 1 complete, awaiting CEO analysis)
- Test baseline: 1259 passed, 0 failed, 5 skipped
- pr_last_merged: 262
- ceo_memory: directive_last=299 (300a is a test run, not a build directive)
- Next: CEO analyzes 1,500 domain distribution, decides ETV window + category selection, then issues Stage 2 (website scraping on discovered domains)
