"""Keiracom System memory layer — wrappers around the Hindsight engine.

V1 primitives per ceo:memory_abstraction_layer_v1 substantive_lock:
  "V1 primitives as thin domain wrappers around Hindsight TEMPR + Opinion/Reflect pathway"

See `wrappers/` for the four MAL-node-typed wrappers + the Trace composition.
"""
